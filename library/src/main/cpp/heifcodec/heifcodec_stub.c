/* Stub source required so CMake accepts a SHARED library target with no direct
   C/C++ source code.  All real symbols are pulled in from the static archive
   dependencies via -Wl,--whole-archive in the link step. */
